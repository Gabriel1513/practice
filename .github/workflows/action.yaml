name: 'Hello World'

on: 
  [push]
      
jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: hello
        run: echo "Hello world"
        
      - name: Checkout
        uses: actions/checkout@v2
        
      - name: test_hello
        uses: ./.github/actions/hello_world
        
